name: TKS Exercise Tracker
on:
  push:
    branches:
      - main
      - development
jobs:
  test:
    name: Testing
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install and test
        run: |
          npm install
          npm run test
  build:
    name: Building
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Building
        id: build
        run: |
          echo "Testing OK... BUILDING > > >"
